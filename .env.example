# Optional environment variables for self-hosted deployments.
# The current alpha stack runs without them, but these are the first
# knobs worth setting once the app is exposed beyond a local test box.

APP_ENV=alpha
CORS_ALLOW_ORIGINS=*
TRUSTED_HOSTS=*
ENABLE_GZIP=true
IMPORT_AUTO_QUEUE_ON_CITY_CREATE=true
